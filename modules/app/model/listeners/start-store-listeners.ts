import { ListenerMiddlewareInstance } from '@reduxjs/toolkit';


export function startStoreListeners(
    listenerMiddleware: ListenerMiddlewareInstance,
) {
    // setupAppListener(listenerMiddleware);

}
